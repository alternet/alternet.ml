/**
 * Some sample interfaces and expected implementations, similar to those generate by the byte code factory.
 * It doesn't matter to have such useless class since this artifact serves only at compile time and is not
 * intended to be bundled with other projects.
 */
package ml.alternet.util.gen.sample;
