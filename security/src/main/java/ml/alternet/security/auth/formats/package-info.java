/**
 * Contains common built-in family crypt formats that
 * allow to extract the hash scheme used for resolving the concrete hasher.
 *
 * <p><strong>It is up to the user to register it to the discovery service</strong>.</p>
 */
@ml.alternet.misc.InfoClass
package ml.alternet.security.auth.formats;
